# Full dated source snapshots — redundant with git history, and large
/Backups/

# Claude Code local session config / permissions
/.claude/

# Host-test suite — kept local only (run offline); not tracked in the repo
/host_test/

# Internal design plans / specs — kept local only; not tracked in the repo
/docs/superpowers/

# One-off debug screenshot of a path-planning bug that's since been fixed
/Path Plan error.jpg

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp
